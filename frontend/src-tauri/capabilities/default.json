{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions par défaut — accès réseau vers le backend AnimeGuesser",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
