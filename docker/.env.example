# ── Ports exposés ──────────────────────────
LB_PORT=80
DASHBOARD_PORT=9000

# ── Base de données ─────────────────────────
DB_ROOT_PASSWORD=rootpassword
DB_DATABASE=animeguesser
DB_USERNAME=animeguesser
DB_PASSWORD=secret

# ── Laravel ────────────────────────────────
# Générer avec : php artisan key:generate --show
APP_KEY=base64:CHANGEME
